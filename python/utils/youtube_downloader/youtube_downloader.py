#!/usr/bin/env python3

import logging
import sys
import argparse
from pytube import Youtube as yt

logging.basicConfig(level=logging.DEBUG, format='[s(asctime)%] [s(levelname)%] - s(message)%')

logger = logging.getLogger(__name__)




